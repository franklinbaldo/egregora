name: CI

# Cancel outdated runs on new pushes to same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Minimal permissions by default
permissions:
  contents: read

env:
  PYTHON_VERSION: "3.12"
  ARTIFACT_RETENTION_DAYS: "7"
  COVERAGE_RETENTION_DAYS: "30"

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:
    inputs:
      debug_enabled:
        description: 'Enable debug logging'
        type: boolean
        default: false
      skip_tests:
        description: 'Skip test jobs (for workflow debugging only)'
        type: boolean
        default: false
      skip_security:
        description: 'Skip security scans (faster feedback)'
        type: boolean
        default: false

jobs:
  pre-commit:
    name: Pre-commit Hooks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Python environment
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          extras: "--all-extras"

      - name: Run pre-commit on all files
        run: |
          uv run pre-commit run --all-files --show-diff-on-failure

  lint-ruff-check:
    name: Ruff (lint)
    runs-on: ubuntu-latest
    steps:
      - name: Enable debug logging
        if: ${{ inputs.debug_enabled }}
        run: |
          echo "ACTIONS_STEP_DEBUG=true" >> $GITHUB_ENV
          echo "ACTIONS_RUNNER_DEBUG=true" >> $GITHUB_ENV
          echo "::notice::Debug logging enabled"

      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Python environment
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          extras: "--all-extras"

      - name: Run ruff (lint)
        run: uv run ruff check src/ tests/

  lint-ruff-format:
    name: Ruff (format)
    runs-on: ubuntu-latest
    needs: [lint-ruff-check]
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Python environment
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          extras: "--all-extras"

      - name: Run ruff (format check)
        run: uv run ruff format --check src/ tests/

  lint-mypy:
    name: Mypy
    runs-on: ubuntu-latest
    needs: [lint-ruff-check, lint-ruff-format]
    steps:
      - name: Setup Python environment
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          extras: "--all-extras"

      - name: Run mypy
        run: uv run mypy src/

  test-unit:
    name: Unit Tests (Python ${{ env.PYTHON_VERSION }})
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: ${{ !inputs.skip_tests }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Python environment
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          extras: "--all-extras"

      - name: Run unit tests
        env:
          GEMINI_API_KEY: "test-key"
        run: |
          uv run pytest tests/unit/ -n auto -v --cov=src --cov-branch --cov-report=xml:coverage.xml --cov-report=term

      - name: Upload coverage to Codecov
        if: github.repository == 'franklinbaldo/egregora'
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          flags: unit
          name: unit-tests
          fail_ci_if_error: false

      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-unit
          path: coverage.xml
          retention-days: ${{ env.COVERAGE_RETENTION_DAYS }}

      - name: Upload test results on failure
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: unit-test-results
          path: |
            .pytest_cache/
            *.log
          retention-days: ${{ env.ARTIFACT_RETENTION_DAYS }}

  test-e2e:
    name: E2E Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [lint-ruff-check, lint-ruff-format, lint-mypy]
    if: ${{ !inputs.skip_tests }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Python environment
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          extras: "--all-extras"

      - name: Run E2E tests with coverage
        env:
          GEMINI_API_KEY: "test-key"
        run: uv run pytest tests/e2e/ -n auto -v --cov=src --cov-branch --cov-report=xml --cov-report=term

      - name: Upload coverage to Codecov
        if: github.repository == 'franklinbaldo/egregora'
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          flags: e2e
          name: e2e-tests
          fail_ci_if_error: false

      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: coverage-e2e
          path: coverage.xml
          retention-days: ${{ env.COVERAGE_RETENTION_DAYS }}

      - name: Upload test results on failure
        if: failure()
        uses: actions/upload-artifact@v6
        with:
          name: e2e-test-results
          path: |
            .pytest_cache/
            *.log
            .egregora/
          retention-days: ${{ env.ARTIFACT_RETENTION_DAYS }}

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: [lint-ruff-check, lint-ruff-format, lint-mypy]
    timeout-minutes: 10
    if: ${{ !inputs.skip_security }}
    permissions:
      contents: read
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Python environment
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          extras: "--all-extras"

      - name: Run safety check
        run: uvx safety check --full-report

      - name: Run pip-audit
        run: uvx pip-audit

      - name: Run bandit (security linter)
        continue-on-error: true
        run: uvx bandit -r src/ -f sarif -o bandit-report.sarif

      - name: Upload Bandit results (SARIF)
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: bandit-report.sarif

  build:
    name: Build Package
    runs-on: ubuntu-latest
    needs: [lint-ruff-check, lint-ruff-format, lint-mypy, test-unit, test-e2e, security]
    if: ${{ !cancelled() && !contains(needs.*.result, 'failure') }}
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Python environment
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          extras: "--all-extras"

      - name: Build package
        run: uv build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v6
        with:
          name: dist
          path: dist/
          retention-days: ${{ env.COVERAGE_RETENTION_DAYS }}

  docs:
    name: Build & Deploy Docs
    needs: [build, security, quality]
    if: ${{ !cancelled() && !contains(needs.*.result, 'failure') }}
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write  # Required for GitHub Pages deployment
    steps:
      - name: Determine if docs should run
        id: docs_condition
        run: |
          if [[ "${{ github.event_name }}" == "push" && "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout code
        if: steps.docs_condition.outputs.run == 'true'
        uses: actions/checkout@v6

      - name: Setup Python environment
        if: steps.docs_condition.outputs.run == 'true'
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          extras: "--extra docs"

      - name: Build Documentation
        if: steps.docs_condition.outputs.run == 'true'
        run: uv run mkdocs build

      - name: Setup Node.js
        if: steps.docs_condition.outputs.run == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - name: Generate Repomix bundles
        if: steps.docs_condition.outputs.run == 'true'
        run: |
          mkdir -p /tmp/bundles
          npx repomix -c repomix-docs.json --output /tmp/bundles/docs.bundle.md
          npx repomix -c repomix-tests.json --output /tmp/bundles/tests.bundle.md
          npx repomix -c repomix-code.json --output /tmp/bundles/code.bundle.md
          mkdir -p site/bundles
          cp /tmp/bundles/*.bundle.md site/bundles/

      - name: Deploy to GitHub Pages
        if: steps.docs_condition.outputs.run == 'true' && github.repository == 'franklinbaldo/egregora'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./site

      - name: Upload bundles artifact
        if: steps.docs_condition.outputs.run == 'true'
        uses: actions/upload-artifact@v6
        with:
          name: bundles
          path: /tmp/bundles/
          retention-days: ${{ env.ARTIFACT_RETENTION_DAYS }}

      - name: Docs build skipped
        if: steps.docs_condition.outputs.run == 'false'
        run: echo "Documentation build skipped for this event."

  quality:
    name: Code Quality Metrics
    runs-on: ubuntu-latest
    needs: [lint-ruff-check, lint-ruff-format, lint-mypy, test-unit]
    timeout-minutes: 10
    steps:
      - name: Determine if quality checks should run
        id: quality_condition
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/main" || "${{ github.event_name }}" == "schedule" ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout code
        if: steps.quality_condition.outputs.run == 'true'
        uses: actions/checkout@v6

      - name: Setup Python environment
        if: steps.quality_condition.outputs.run == 'true'
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          extras: "--all-extras"

      - name: Run radon (complexity)
        if: steps.quality_condition.outputs.run == 'true'
        run: uv run radon cc src/ -a

      - name: Run xenon (threshold enforcement)
        if: steps.quality_condition.outputs.run == 'true'
        continue-on-error: true
        run: uv run xenon src/ --max-absolute B --max-modules B --max-average B

      - name: Quality checks skipped
        if: steps.quality_condition.outputs.run == 'false'
        run: echo "Quality checks skipped for this event."

  summary:
    name: Workflow Summary
    runs-on: ubuntu-latest
    needs: [pre-commit, lint-ruff-check, lint-ruff-format, lint-mypy, test-unit, test-e2e, security, quality, build, docs]
    if: always()
    steps:
      - name: Post summary
        run: |
          cat <<'EOF' >> $GITHUB_STEP_SUMMARY
          ## Workflow Summary

          | Job | Result |
          | --- | ------ |
          | Pre-commit Hooks | ${{ needs['pre-commit'].result }} |
          | Ruff Lint | ${{ needs['lint-ruff-check'].result }} |
          | Ruff Format | ${{ needs['lint-ruff-format'].result }} |
          | Mypy | ${{ needs['lint-mypy'].result }} |
          | Unit Tests | ${{ needs['test-unit'].result }} |
          | E2E Tests | ${{ needs['test-e2e'].result }} |
          | Security | ${{ needs.security.result || 'skipped' }} |
          | Quality | ${{ needs.quality.result || 'skipped' }} |
          | Build | ${{ needs.build.result || 'skipped' }} |
          | Docs | ${{ needs.docs.result || 'skipped' }} |
          EOF
