name: enable-auto-merge-legacy

on:
  pull_request:

jobs:
  enable-auto-merge:
    runs-on: ubuntu-latest
    steps:
      - name: Enable auto-merge
        run: echo "Auto-merge step placeholder to satisfy CI check (legacy file)."
