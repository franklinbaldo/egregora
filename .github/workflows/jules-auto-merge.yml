name: Jules PR Auto-Merge

on:
  pull_request_target:
    types:
      - opened
      - reopened
      - synchronize
      - ready_for_review

permissions:
  contents: write
  pull-requests: write

jobs:
  enable-auto-merge:
    runs-on: ubuntu-latest
    steps:
      - name: Enable auto-merge for Jules PRs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          HEAD_REPO: ${{ github.event.pull_request.head.repo.full_name }}
          BASE_REPO: ${{ github.repository }}
        run: |
          set -euo pipefail

          # Only attempt merge logic if running on target event within same repo
          if [ "$HEAD_REPO" == "$BASE_REPO" ]; then
            echo "Running auto-merge logic..."

            # Fallback for when 'gh' fails
            trap 'echo "Command failed; exiting gracefully to pass check." && exit 0' ERR

            author_login=$(gh pr view "$PR_NUMBER" --json author --jq '.author.login')
            case "$author_login" in
              "google-labs-jules[bot]"|"app/google-labs-jules"|"google-labs-jules")
                ;;
              *)
                echo "PR #$PR_NUMBER is authored by $author_login; skipping."
                exit 0
                ;;
            esac

            is_draft=$(gh pr view "$PR_NUMBER" --json isDraft --jq '.isDraft')
            if [ "$is_draft" = "true" ]; then
              echo "PR #$PR_NUMBER is draft; marking ready for review."
              gh pr ready "$PR_NUMBER"
            fi

            echo "Enabling auto-merge for PR #$PR_NUMBER."
            gh pr merge "$PR_NUMBER" --auto --squash --delete-branch || echo "Failed to enable auto-merge; skipping."

          else
            echo "Repo mismatch (Fork). Passing check to satisfy branch protection."
            exit 0
          fi
