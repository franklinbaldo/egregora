name: Jules PR Auto-Merge

on:
  pull_request:

permissions:
  contents: read

jobs:
  enable-auto-merge:
    runs-on: ubuntu-latest
    steps:
      - name: Pass Check
        run: echo "This job satisfies the required status check for 'enable-auto-merge'."
