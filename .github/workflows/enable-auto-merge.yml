name: enable-auto-merge

on:
  pull_request:
<<<<<<< HEAD

permissions:
  contents: read

jobs:
  enable-auto-merge:
    runs-on: ubuntu-latest
    name: enable-auto-merge
    steps:
      - run: echo "Auto-merge check satisfied"
=======
  pull_request_target:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

jobs:
  enable-auto-merge:
    name: enable-auto-merge
    runs-on: ubuntu-latest
    steps:
      - name: Enable Auto-merge Check
        run: echo "Auto-merge check satisfied."
>>>>>>> origin/pr/2879
