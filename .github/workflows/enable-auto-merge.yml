name: enable-auto-merge

on:
  pull_request:

jobs:
  enable-auto-merge:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Dummy Check
        run: |
          echo "This check satisfies the 'enable-auto-merge' status requirement."
          sleep 90
