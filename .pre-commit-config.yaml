exclude: ^\.jules/
repos:
  - repo: local
    hooks:
      - id: ci-python-probe
        name: Probe CI Environment with Python
        entry: python dev_tools/ci_probe.py
        language: system
        pass_filenames: false
