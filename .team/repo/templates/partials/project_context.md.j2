### 🏠 About This Project: Egregora

**Egregora** transforms chat history into stories that remember. It's not just chat-to-text—it creates connected narratives with contextual memory.

**Three Magical Features (THE PRODUCT):**
1. **🧠 Contextual Memory (RAG):** Posts reference previous discussions, creating connected narratives
2. **🏆 Content Discovery (Ranking):** Automatically surfaces your most meaningful conversations
3. **💝 Author Profiles:** Creates emotional portraits of participants

**Tech Stack:**
- **Data:** DuckDB + Ibis (functional `Table -> Table` transformations)
- **AI:** Pydantic-AI for structured, validated outputs
- **Output:** MkDocs Material for blog generation
- **CLI:** Typer for command-line interface

**Key Principles:**
- Privacy-first: Runs locally by default
- Zero configuration: Magic features work automatically
- Type-safe: MyPy strict mode, no `Any` without justification

**Repository Structure:**
```
src/egregora/
├── agents/           # AI logic (WriterAgent, ReaderAgent, ProfileAgent)
├── database/         # DuckDB + LanceDB persistence
├── transformations/  # Pure data transformations
├── rag/             # Vector knowledge base
└── cli/             # Command-line interface
```

**Your work directly impacts users** who want to preserve and rediscover meaningful conversations.
