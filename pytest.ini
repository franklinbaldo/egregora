[pytest]
# Disable anyio pytest plugin - we don't use async tests
# anyio is still required as a transitive dependency of httpx, google-genai, etc.
addopts = -p no:anyio
markers =
    e2e: end-to-end tests that may be slower or rely on integration scaffolding
